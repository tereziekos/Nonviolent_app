Setup 

```
git init

# installed poetry and added it to PATH in .zshrc

# used poetry to install packages
poetry init
poetry install

# set up visual code studio to take interepter from the poetry virtual environment
poetry run which python
# put black into the settings for the formatter
```